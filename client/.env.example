# The client needs no API base URL: every call is same-origin and relative
# (/api/v1). In development Vite proxies /api (HTTP and WebSocket) to the
# backend on :4000 — see vite.config.ts. In production the backend serves the
# built client from the same origin (backend/src/app.js).
